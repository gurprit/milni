import {NextResponse} from 'next/server';
import {d1Execute,d1Query} from '../../../../../lib/d1';
import {currentOrganiserSession} from '../../../../../lib/organiserSession';
type Row={event_id:string;status:string;total:number};
type GuestRow={event_id:string;status:string;guest_id:string;name:string;guest_group:string|null};
export async function GET(_request:Request,{params}:{params:Promise<{slug:string}>}){try{const organiser=await currentOrganiserSession();if(!organiser)return NextResponse.json({ok:false,error:'Organiser sign-in required.'},{status:401});const{slug}=await params;const wedding=(await d1Query<{id:string}>('SELECT id FROM weddings WHERE slug=? LIMIT 1',[slug]))[0];if(!wedding)return NextResponse.json({ok:false,error:'Wedding not found.'},{status:404});await d1Execute('CREATE TABLE IF NOT EXISTS event_rsvps (wedding_id TEXT NOT NULL, guest_id TEXT NOT NULL, event_id TEXT NOT NULL, status TEXT NOT NULL, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, PRIMARY KEY (guest_id,event_id))');const rows=await d1Query<Row>('SELECT event_id,status,COUNT(*) AS total FROM event_rsvps WHERE wedding_id=? GROUP BY event_id,status',[wedding.id]);const guests=await d1Query<GuestRow>('SELECT r.event_id,r.status,r.guest_id,g.name,g.guest_group FROM event_rsvps r JOIN guests g ON g.id=r.guest_id AND g.wedding_id=r.wedding_id WHERE r.wedding_id=? ORDER BY g.name COLLATE NOCASE',[wedding.id]);const summaries:Record<string,{going:number;maybe:number;notGoing:number;responded:number;guests:{id:string;name:string;group:string;status:string}[]}>={};for(const row of rows){const item=summaries[row.event_id]??{going:0,maybe:0,notGoing:0,responded:0,guests:[]};const total=Number(row.total)||0;if(row.status==='Going')item.going=total;else if(row.status==='Maybe')item.maybe=total;else if(row.status==='Not going')item.notGoing=total;item.responded+=total;summaries[row.event_id]=item}for(const guest of guests){const item=summaries[guest.event_id]??{going:0,maybe:0,notGoing:0,responded:0,guests:[]};item.guests.push({id:guest.guest_id,name:guest.name,group:guest.guest_group??'',status:guest.status});summaries[guest.event_id]=item}return NextResponse.json({ok:true,summaries})}catch(error){return NextResponse.json({ok:false,error:error instanceof Error?error.message:'Could not load attendance summaries.'},{status:500})}}
