'use client';import Link from 'next/link';import {useParams} from 'next/navigation';import InvitationManager from './InvitationManager';import styles from './page.module.scss';export default function InvitationManagerPage(){const params=useParams();const slug=String(params.slug||'');return <main className={styles.invitePage}><header><Link href={`/wedding/${slug}`}>MILNI <small>PEOPLE · TRADITIONS · TOGETHER</small></Link><Link href={`/wedding/${slug}`}>← Wedding dashboard</Link></header><InvitationManager/></main>}