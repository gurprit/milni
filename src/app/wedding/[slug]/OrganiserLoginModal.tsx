'use client';
import {useState} from 'react';
import styles from './dashboard.module.scss';
export default function OrganiserLoginModal({onClose,onSuccess}:{onClose:()=>void;onSuccess:()=>void}){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const login=async()=>{setError('');setBusy(true);try{const response=await fetch('/api/organiser-session',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const result=await response.json();if(!response.ok){setError(result.error||'Could not sign in.');return}onSuccess()}catch{setError('Could not sign in. Please try again.')}finally{setBusy(false)}};return <div className={styles.rsvpOverlay} onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className={styles.organiserLogin}><button className={styles.rsvpClose} onClick={onClose} aria-label="Close">×</button><div className={styles.eyebrow}>ORGANISER ACCESS</div><h2>Welcome back</h2><p>Sign in to edit the wedding and view private organiser information.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" autoFocus/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void login()}} autoComplete="current-password"/></label>{error&&<div className={styles.rsvpError}>{error}</div>}<button className={styles.organiserLoginButton} disabled={busy} onClick={()=>void login()}>{busy?'Signing in…':'Sign in as organiser'}</button></section></div>}
