'use client';

import { WeddingDraft, TravelItem, VenueInfo } from '../../../lib/weddingDraft';
import styles from './travel.module.scss';

const starterTravel:TravelItem[]=[
 {id:'travel-coach-gurdwara',name:'Coach to the Gurdwara',time:'09:00',place:'Hotel entrance',destination:'Gurdwara',note:'Seats reserved for wedding guests'},
 {id:'travel-coach-reception',name:'Coach to reception',time:'15:30',place:'Gurdwara car park',destination:'Reception',note:'Departs after photographs'},
 {id:'travel-return',name:'Return coaches',time:'00:00',place:'Reception entrance',destination:'Hotel',note:'Late-night return service'}
];
const emptyVenue:VenueInfo={name:'Wedding venue',address:'',parking:'',mapUrl:'',taxiInfo:''};

export default function TravelManager({draft,save,organiser}:{draft:WeddingDraft;save:(p:Partial<WeddingDraft>)=>void;organiser:boolean}){
 const travel=draft.travel?.length?draft.travel:starterTravel;
 const venue=draft.venue??emptyVenue;
 const events=(draft.schedule??[]).flatMap(day=>day.events.map(event=>({id:event.id,label:`${day.label} · ${event.start} · ${event.name}`})));
 const persistTravel=(next:TravelItem[])=>save({travel:next});
 const updateTravel=(id:string,patch:Partial<TravelItem>)=>persistTravel(travel.map(item=>item.id===id?{...item,...patch}:item));
 const addTravel=()=>persistTravel([...travel,{id:`travel-${Date.now()}`,name:'New journey',time:'10:00',place:'Pickup point',destination:'Destination',note:'',linkedEventId:''}]);
 const removeTravel=(id:string)=>{if(window.confirm('Remove this journey?'))persistTravel(travel.filter(item=>item.id!==id))};
 const updateVenue=(patch:Partial<VenueInfo>)=>save({venue:{...venue,...patch}});

 if(!organiser)return <div className={styles.travelGuestGrid}><section className={styles.card}><div className={styles.eyebrow}>GETTING AROUND</div><h2>Wedding transport</h2>{travel.map(item=><div className={styles.infoRow} key={item.id}><strong>{item.time}</strong><span><h3>{item.name}</h3><p>{item.place}{item.destination?` → ${item.destination}`:''}</p>{item.note&&<p>{item.note}</p>}</span></div>)}</section><section className={`${styles.card} ${styles.tint}`}><div className={styles.eyebrow}>VENUE</div><h2>{venue.name||'Venue & directions'}</h2><div className={styles.map}>◇<span>{venue.address||'Wedding venue map'}</span></div>{venue.address&&<p>{venue.address}</p>}{venue.parking&&<><h3>Parking</h3><p>{venue.parking}</p></>}{venue.taxiInfo&&<><h3>Taxi information</h3><p>{venue.taxiInfo}</p></>}{venue.mapUrl&&<a className={styles.mapLink} href={venue.mapUrl} target="_blank" rel="noreferrer">Open map ↗</a>}</section></div>;

 return <div className={styles.travelAdmin}><div className={styles.editorIntro}><div><strong>Manage travel</strong><span>Keep coaches, pickup points, venue directions and late-night journeys in one place.</span></div><button onClick={addTravel}>＋ Add journey</button></div><div className={styles.travelAdminGrid}><section className={styles.travelJourneys}><div className={styles.travelSectionHead}><div><div className={styles.eyebrow}>TRANSPORT</div><h2>Guest journeys</h2></div><span>{travel.length} journey{travel.length===1?'':'s'}</span></div>{travel.map(item=><article className={styles.travelEditorCard} key={item.id}><div className={styles.travelEditorTop}><label>Journey name<input value={item.name} onChange={e=>updateTravel(item.id,{name:e.target.value})}/></label><label>Departure<input type="time" value={item.time} onChange={e=>updateTravel(item.id,{time:e.target.value})}/></label></div><div className={styles.travelEditorRoute}><label>Pickup point<input value={item.place} placeholder="Hotel entrance" onChange={e=>updateTravel(item.id,{place:e.target.value})}/></label><span>→</span><label>Destination<input value={item.destination??''} placeholder="Reception venue" onChange={e=>updateTravel(item.id,{destination:e.target.value})}/></label></div><label>Guest notes<textarea rows={2} value={item.note} placeholder="Where to meet, who this coach is for, accessibility notes…" onChange={e=>updateTravel(item.id,{note:e.target.value})}/></label><div className={styles.travelEditorFooter}><label>Link to schedule event<select value={item.linkedEventId??''} onChange={e=>updateTravel(item.id,{linkedEventId:e.target.value})}><option value="">No linked event</option>{events.map(event=><option value={event.id} key={event.id}>{event.label}</option>)}</select></label><button className={styles.removeButton} onClick={()=>removeTravel(item.id)}>Remove journey</button></div></article>)}<button className={styles.addEventButton} onClick={addTravel}>＋ Add another journey</button></section><aside className={styles.venueEditor}><div className={styles.eyebrow}>VENUE & DIRECTIONS</div><h2>Guest arrival information</h2><p>These details appear on the guest Travel page alongside the coach plan.</p><label>Venue name<input value={venue.name} onChange={e=>updateVenue({name:e.target.value})}/></label><label>Address<textarea rows={3} value={venue.address} placeholder="Full venue address" onChange={e=>updateVenue({address:e.target.value})}/></label><label>Map link<input type="url" value={venue.mapUrl} placeholder="Map link" onChange={e=>updateVenue({mapUrl:e.target.value})}/></label><label>Parking instructions<textarea rows={3} value={venue.parking} placeholder="Where guests should park, permits, drop-off…" onChange={e=>updateVenue({parking:e.target.value})}/></label><label>Taxi / local transport<textarea rows={3} value={venue.taxiInfo} placeholder="Recommended taxi firms, station information…" onChange={e=>updateVenue({taxiInfo:e.target.value})}/></label>{venue.mapUrl&&<a className={styles.mapLink} href={venue.mapUrl} target="_blank" rel="noreferrer">Open map ↗</a>}</aside></div></div>;
}
